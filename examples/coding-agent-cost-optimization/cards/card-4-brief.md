# Card 4 · 落地路径

**核心问题**：按什么顺序做？能省多少？

**6 步阶梯**（按投入产出排序）：

Step 0 · 观测（免费）：装 ccusage / tokscale，先看清楚钱花在哪里
Step 1 · 缓存纪律（一次配置）：固定 system prompt 顺序、清理不用的 MCP、关 attribution header → 节省 ~45%
Step 2 · 减少噪音（半天）：写好 CLAUDE.md 路标、静态语言项目开 LSP → session token 减 30%
Step 3 · 预防膨胀（装工具）：装 RTK 压缩 CLI 输出 → session 再瘦 15%
Step 4 · 隔离（改习惯）：重约探索任务给 Haiku sub-agent，不同任务间 /clear → 探索成本便宜 15x
Step 5 · 外置记忆（改工作流）：复杂任务先生成 SPEC.md，长任务用 planning-with-files

**成本弧线**：$11.30（什么都不做）→ $4-5（全部措施到位）

**布局**：有序步骤列表（Step 0-5），每步右侧标注累计节省，底部收口展示最终数字对比
